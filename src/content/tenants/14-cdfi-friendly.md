---
name: 'CDFI Friendly'
# ⚠️ UNCONFIRMED. `category` records which wing a tenant occupies, and nobody has
# said which one this is. `office` is the guess a finance organisation invites,
# and the field is not rendered — the wing split stopped being a display control
# when the two groups merged into one grid — so a wrong guess costs nothing on
# screen. It is still a fact in the data, so it wants confirming.
category: office
# The supplied blurb, used as the tile's `meta` line. Longer than the two-word
# `type` most tenants carry, but MarkTile takes a tenant's `type` and a
# neighbour's `blurb` through the same slot, and the neighbour tiles are all full
# sentences — so this renders exactly as those do.
type: >-
  Community investment lender
logo: ../../assets/marks/cdfi.png
imageAlt: >-
  CDFI Friendly logo: "CDFI" in white on a yellow-green block beside "Friendly"
  in white on a pale blue block, above "SBR" and "South Bend Region" in navy.
url: https://cdfifriendlysouthbend.org
featured: true
# 9, so it lands after Brick Biz (8) and before "A Place for You" (99), which is
# pinned last as the invitation that closes the row.
order: 9
placeholder: false
draft: false
# Name, blurb, URL and mark supplied by Rob 2026-09-03. The mark was normalised
# to the 1200x700 house canvas — see the note in the summary; the original is
# 1024x1024 and was a 0.71x downscale, so nothing was softened.
---

Community development finance.
